---
title: Read-Only Mode
sidebar_position: 12
---

# Read-Only Mode

Imported datasets are read-only by default. When a dataset is read-only, all editing actions are disabled in the UI, menu entries switch to their **View** variants, and the save buttons in every dialog are hidden.

To make changes, select the dataset and use **Enable Editing** in the top-right corner, **Edit → Enable Editing** in the menu bar, or **Enable Editing** from the dataset context menu in the navigation tree.

If you want to lock the dataset again later, disable editing from the same places. Disabling editing does not revert existing changes; it only prevents further edits.
